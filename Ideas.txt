# tmp_filter = tmp[0] == max_row[0]
# tmp_filter  = pd.DataFrame(tmp_filter)

# for i in range(len(max_row)):
#     tmp_filter = tmp[i] == max_row[i]

def strategy_scores(population, memory, table):
    """
    Compute the scores of all strategies in the population.
    """
    position = position_straegy(table, memory)

position_straegy(table, tuple(memory[-m:]))

def evaluate_strategy(strategy, memory):
    """
    Evaluate a strategy with a memory.
    """
    

np.array(np.random.randint(2, size=2**m), ndmin=3)
